<%= form_for :item, url: item_url, remote: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.label :comment %>
  <%= f.text_field :comment %>

  <%= f.submit %>
<% end %>
